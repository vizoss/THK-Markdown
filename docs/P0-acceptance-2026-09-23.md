# P0 模拟器验收记录（2026-09-23）

环境：正在运行的 iPhone 17 / iOS 26.5 示例、Android Studio 内 Pixel 9 API 36 示例。通过桌面 UI 实际操作，不是仅检查源码。当前运行包由维护者启动，本轮尚未重新构建；不包含 CocoaPods 路径或真机认证。

记录只覆盖明确列出的操作；未记录的主题、字体缩放、横屏、复制保真及流式中间态均不能视为通过。截图为本任务工具调用中的现场画面，尚未建立批准的截图基线。

## P0-01 单层引用与复制

- 双端：检查全文、单步首片、继续播放到 3/3；正文自然折行，复制按钮位于右侧，无独立按钮行或遮字。
- iOS：点击复制，粘贴到示例输入框，确认内容为“第一行引用，这段文字用于窄屏换行。 第二行引用。”，随后清空输入框，未发送。
- Android：点击复制，系统剪贴板/分享预览显示对应两句引用文字；未向任何应用发送。尚未逐字符回读 Android 剪贴板。
- 结论：当前默认主题下已查的显示和播放项通过；Android 逐字符复制校验、其他主题仍待检查。

## P0-02 二级引用间距

- 双端实际选择用例，检查全文并播放至 5/5 完成。
- 外层竖条连续，内层两段分行；二级上下留白可见，外层结尾恢复原缩进，按钮未遮字。
- 播放完成态与全文肉眼一致；未做逐帧录制或屏幕物理像素测量。
- 结论：当前默认主题的上述视觉回归项通过，其他主题和复制保真未在本项重测。

## P0-03 只有嵌套引用

- 双端检查全文、播放至 3/3：三级竖条不重叠，二级覆盖两行内容，外层条包含嵌套留白；底部背景不再紧贴外层竖条。
- iOS 本项提交之前补好的末段留白修复：将末段段后间距移至文本容器底部 inset，并扩展背景绘制区域；复用时重置。回归测试已补，尚未执行单元测试。
- 结论：当前默认主题全文与播放完成态的引用几何回归项通过。其他主题和逐字符复制仍待检查。

## P0-04 独立代码与空行复制

- 双端全文、播放至 4/4：前后文完整；两行代码之间可见空行，按钮在代码右侧，无遮挡。
- iOS 点击复制并粘贴，得到两行代码内容、不含前后文。示例输入框为单行，无法据此认证换行字节保真；此项仍须多行粘贴目标或自动化测试补验。
- 结论：默认主题布局/播放项通过；复制空行逐字符保真尚未完成，不标为完整通过。

## P0-05 引用中的代码

- 发现并修复：iOS 先画代码再画引用导致代码背景被盖住；改为父背景先画，且引用容器边缘 padding 只扩展引用背景，不扩展代码背景。
- Android 按引用/代码源范围顺序布置复制按钮，与 iOS 一致；不再让先收集的内层代码把外层按钮挤向左侧。新增按钮顺序/复制载荷回归测试，尚未运行该单元测试。
- 已通过 Xcode 和 Android Studio 的运行操作重新构建并启动示例，现场复看 P0-05：代码底色可见、两个入口无遮挡，双端播放至 6/6 后正常。
- iOS 两个入口实际粘贴分别得到整段引用和 `let value = 42`。Android 复制载荷未在本项逐字符回读。
- 结论：本项发现的背景遮盖、按钮顺序问题已修复并现场复验；不同平台代码背景的横向范围仍有差异，未宣称像素级一致。
- 附记：Xcode 控制台存在示例启动时 table view 尚未入窗口的布局警告，以及用例选择弹层约束警告，待单独排查。命令行 `xcodebuild -list` 因沙箱缓存权限失败，不是本次 IDE 构建失败。

## P0-06 列表中的代码

- 双端全文及播放到 5/5：步骤一/二均保留项目符号，代码独立缩进和底色可见，右侧复制按钮不遮字，步骤二未进入代码底色。
- 结论：当前默认主题上述布局与完成态通过；本项未重复验证复制载荷。未发现需要新增的代码修复。

## P0-07 三级列表缩进

- 发现 iOS 列表项内部使用双换行拼接，进入子列表额外出现空白行；SPM/CocoaPods 两个 renderer 改用列表内部单换行，保持已有缩进逻辑。补充不插入空段落的断言（未运行单元测试）。
- 已通过 Xcode 重新运行 SPM 示例，解锁后现场复验全文和 4/4 播放完成态：额外空行消失，三级依次缩进、返回一级恢复原位置。Android 全文/4/4 完成态同样保留层级。
- 结论：默认主题当前用例的层级与额外空行问题通过；CocoaPods 仅同步修改，未运行验证，未验证窄屏续行。

## P0-08 有序与任务列表混合

- 双端播放至 4/4：外层 1/2 编号完整，已完成/待完成复选框各一个且状态正确，不重复外层编号，结束项恢复外层位置。
- 结论：当前默认主题短行用例的完成态通过；复选框两端绘制风格有差异，未验证长行续行和其他主题。本项无需代码修复。

## P0-09 列表中的引用

- 双端全文及 4/4 播放完成态：列表项目完整；引用保留两级竖条和缩进、右侧复制入口；列表结尾恢复外层位置，没有引用竖条或背景串入。
- 结论：上述默认主题布局项通过，未额外复验复制载荷。与独立引用一样，两端背景横向覆盖范围存在差异，未做像素级一致性认证。

## P0-10 表格内格式与链接

- 双端全文及播放至 3/3：两列表头/数据完整，粗体与行内代码可辨，左右对齐与边框正常，未出现重叠。
- 实际点击单元格链接：iOS 状态栏、Android Toast 均反馈 `https://example.com/p0`，未跳出示例。
- 结论：当前短表格的布局、链接回调与完成态通过；表格没有超出容器，故本例未覆盖横向滚动。无需代码修复。
