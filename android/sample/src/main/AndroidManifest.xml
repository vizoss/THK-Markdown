<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed for DefaultTHKImageLoader's HttpURLConnection fetches (the image-showcase
         mock reply loads a real https://picsum.photos URL). -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.THKMDViewSample">
        <activity android:name=".ShowCaseActivity" android:exported="false" />
        <activity android:name=".SSEChatActivity" android:exported="false" android:windowSoftInputMode="adjustResize" />
        <activity android:name=".ThemeSettingsActivity" android:exported="false" />
        <activity android:name=".WeeklyListActivity" android:exported="false" />
        <activity android:name=".WeeklyDetailActivity" android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
