org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Base version for the :thkmdview publication. CI appends a build qualifier per push
# to main (see .github/workflows/android-publish.yml) since GitHub Packages will not
# let you republish an already-existing version.
VERSION_NAME=0.1.0
